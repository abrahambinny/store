{% extends 'article/base.html' %}
{% load staticfiles %}
{% load article_filter %}
<!DOCTYPE HTML>
<html>
  <head>
    <title>{% block title %}Navigatio Home{% endblock %}</title>
    <!-- block meta_header -->
    <!-- block header_js -->
    <!-- block header_css -->
  </head>
  <body>
    <!-- Wrapper -->
    <div id="wrapper">
      <!-- Header -->
      <!-- block header -->
      <!-- Menu -->
      <!-- block menu -->
      
      <!-- Main -->
      <div id="main">
        {% block content %}
        <!-- Post -->
        <article class="post">
          <h2><a href="/detail/{{main_article.id}}/">{{main_article.title}}</a></h2>
           <footer>
            <ul class="stats">
              <li title="category"><b>{{main_article.category}}</b></li>
              <li title="published date"><time class="published" datetime="{{main_article.pub_date}}">{{main_article.pub_date|date:"l F d, Y"}}</time></li>
              <li title="author"><a href="#"><span class="name">{{main_article.author.author_name}}</span></a></li>
            </ul>
         </footer>
          <a href="/detail/{{main_article.id}}/" class="image featured"><img src="{% static '' %}{{main_article.image.main_img.name|fetch_img_url}}" alt="" /></a>
          <p>{{main_article.description|truncatewords:100}}</p>
          <footer>
            <ul class="actions">
              <li><a href="/detail/{{main_article.id}}/" class="button big">Continue Reading</a></li>
            </ul>
          </footer>
        </article>

        <!-- Post -->
        {% for article in other_article %}
        <article class="post">
          <h2><a href="/detail/{{article.id}}/">{{article.title}}</a></h2>
          <p>{{article.description|truncatewords:50}}</p>
          <footer>
            <ul class="actions">
              <li><a href="/detail/{{article.id}}/" class="button big">Continue Reading</a></li>
            </ul>
          </footer>
        </article>
        {% endfor %}

        <!-- Pagination -->
        <ul class="actions pagination">
          <li><a href="" class="disabled button big previous">Previous Page</a></li>
          <li><a href="#" class="button big next">Next Page</a></li>
        </ul>
        {% endblock %}
        <!-- block next_content --> 
      </div>

      <!-- block next_content -->

      <!-- Sidebar -->
      <!-- block sidebar -->
        <!-- Intro -->
        <!-- block intro -->
        <!-- Mini Posts -->
        <!-- block mini_posts -->
        <!-- block posts_list -->
        <!-- About -->
        <!-- block about -->
        <!-- Footer -->
        <!-- block footer -->
      <!-- end block sidebar -->
    </div>
    <!-- Scripts -->
    <!-- block footer_js -->
  </body>
</html>
